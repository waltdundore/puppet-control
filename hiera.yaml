version: 5
defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "YAML hierarchy levels"
    paths:
      - "nodes/%{::fqdn}.yaml"
      - "%{::domain}.yaml"
      - "common.yaml"
      - "users.yaml"
